import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, ArrowRight, CheckCircle2, Clock3, FileText, LockKeyhole, Plus, Search, ShieldCheck } from "lucide-react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { collection, doc, getDocs, onSnapshot, query, where } from "firebase/firestore";
import { DashboardLayout } from "../Components/Dashboard";
import { auth, db } from "../firebase";

const progressStages = ["Deal Created", "Seller Accepted", "Payment Secured", "Awaiting Delivery", "Buyer Confirmation", "Payment Release"];
const statusLabels = { awaiting_seller: "Waiting for Seller", awaiting_buyer: "Waiting for Buyer", waiting_for_payment: "Waiting for Payment", payment_secured: "Payment Secured", awaiting_delivery: "Awaiting Delivery", awaiting_confirmation: "Awaiting Confirmation", ready_for_release: "Ready for Release", completed: "Completed", disputed: "Disputed", cancelled: "Cancelled" };
const progressIndex = { "Waiting for Seller": 1, "Waiting for Buyer": 1, "Waiting for Payment": 2, "Payment Secured": 3, "Awaiting Delivery": 3, "Awaiting Confirmation": 4, "Ready for Release": 5, Completed: 6 };
function labelStatus(status) { return statusLabels[status] || status || "Awaiting review"; }
function formatDate(value) { if (!value) return "Date unavailable"; const date = value.toDate ? value.toDate() : new Date(value); return Number.isNaN(date.getTime()) ? "Date unavailable" : date.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }); }
function amount(value) { return `GHS ${Number(value || 0).toLocaleString("en-GH", { minimumFractionDigits: 2 })}`; }
function normalizeDeal(snapshot) { const data = snapshot.data(); return { ...data, documentId: snapshot.id, id: data.transactionCode || `DS-${snapshot.id.slice(0, 5).toUpperCase()}`, name: data.title || "Untitled deal", role: data.creatorRole === "seller" ? "Seller" : "Buyer", otherParty: data.otherParty?.fullName || "Other party", amount: amount(data.amount), status: labelStatus(data.status), date: formatDate(data.createdAt) }; }
function StatusBadge({ status }) { return <span className="inline-flex items-center gap-1.5 rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-bold text-slate-600"><span className="h-1.5 w-1.5 rounded-full bg-current" />{status}</span>; }
function TransactionDetails({ transaction }) {
  const navigate = useNavigate();
  const [accepted, setAccepted] = useState(transaction.invitationAccepted === true);
  const waiting = ["Waiting for Seller", "Waiting for Buyer"].includes(transaction.status);
  const isCreator = transaction.creatorId === auth.currentUser?.uid;
  const isBuyer = transaction.creatorRole === "buyer" ? isCreator : !isCreator;
  const canAccept = waiting && !accepted && !isCreator;
  const canPay = accepted && transaction.status === "Waiting for Payment" && isBuyer;
  const completedStage = progressIndex[transaction.status] ?? 0;
  const accept = () => { setAccepted(true); navigate(`/dashboard/transactions/${transaction.documentId}/payment`); };
  return <div className="mx-auto max-w-4xl">
    <Link className="mb-6 inline-flex items-center gap-2 text-xs font-bold text-[#0b776d]" to="/dashboard/transactions"><ArrowLeft size={15} /> All transactions</Link>
    <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-[11px] font-bold uppercase tracking-[1.2px] text-[#0b776d]">Transaction details</p><h2 className="mt-2 font-display text-2xl font-bold">{transaction.name}</h2><p className="mt-1 text-xs text-[#82938f]">{transaction.id} · Created {transaction.date}</p></div><StatusBadge status={transaction.status} /></div>
    {canAccept && <section className="mb-6 rounded-xl border border-[#bfdfd0] bg-[#eff9f4] p-6"><div className="flex gap-3"><ShieldCheck className="shrink-0 text-[#0b776d]" size={22} /><div><h3 className="font-display text-base font-bold">Review this invitation</h3><p className="mt-2 text-xs leading-5 text-[#5d786f]">Accept the deal before payment can proceed. The invitation does not charge you.</p><button type="button" onClick={accept} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-[#0b776d] px-4 py-3 text-xs font-bold text-white">Accept invitation <CheckCircle2 size={15} /></button></div></div></section>}
    {canPay && <section className="mb-6 flex flex-col justify-between gap-4 rounded-xl border border-[#bfdfd0] bg-[#eff9f4] p-5 sm:flex-row sm:items-center"><p className="text-xs leading-5 text-[#4f7169]"><strong>Invitation accepted.</strong> Pay the agreed amount into escrow to start the protected transaction.</p><Link className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#0b776d] px-4 py-3 text-xs font-bold text-white" to={`/dashboard/transactions/${transaction.documentId}/payment`}>Pay now to escrow <LockKeyhole size={15} /></Link></section>}
    {waiting && isCreator && !canPay && <section className="mb-6 flex items-center gap-3 rounded-xl border border-[#dce7e2] bg-white p-5"><Clock3 className="shrink-0 text-[#ad7b1d]" size={20} /><p className="text-xs leading-5 text-[#617a74]"><strong>{accepted ? "Invitation accepted." : "Invitation sent."}</strong> {accepted ? "The buyer can now pay to escrow." : "Waiting for the other party to review and accept this deal."}</p></section>}
    <section className="rounded-xl border border-[#e1ebe6] bg-white p-5 shadow-sm sm:p-7"><h3 className="font-display text-base font-bold">Transaction progress</h3><div className="mt-7 grid gap-4">{progressStages.map((stage, index) => <div className="relative flex items-center gap-3" key={stage}><span className={`grid h-8 w-8 shrink-0 place-items-center rounded-full text-[11px] font-bold ${index < completedStage ? "bg-[#d9f1e4] text-[#0b776d]" : index === completedStage ? "bg-[#fff0c9] text-[#ad7b1d]" : "bg-[#f1f5f3] text-[#96a6a2]"}`}>{index < completedStage ? <CheckCircle2 size={16} /> : index + 1}</span><p className={`text-xs font-bold ${index === completedStage ? "text-[#ad7b1d]" : "text-[#526d67]"}`}>{stage}</p>{index < progressStages.length - 1 && <span className="absolute left-4 top-8 h-5 border-l border-dashed border-[#cbdad4]" />}</div>)}</div></section>
    <section className="mt-6 rounded-xl border border-[#e1ebe6] bg-white p-5 shadow-sm sm:p-7"><h3 className="font-display text-base font-bold">Deal summary</h3><dl className="mt-5 grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-xs text-[#82938f]">Your role</dt><dd className="mt-1 font-bold">{isBuyer ? "Buyer" : "Seller"}</dd></div><div><dt className="text-xs text-[#82938f]">Other party</dt><dd className="mt-1 font-bold">{transaction.otherParty}</dd></div><div><dt className="text-xs text-[#82938f]">Deal amount</dt><dd className="mt-1 font-bold">{transaction.amount}</dd></div><div><dt className="text-xs text-[#82938f]">Source</dt><dd className="mt-1 font-bold">{transaction.source || "Not provided"}</dd></div><div className="sm:col-span-2"><dt className="text-xs text-[#82938f]">Description</dt><dd className="mt-1 whitespace-pre-line leading-6">{transaction.description || "Not provided"}</dd></div></dl></section>
  </div>;
}
function TransactionCard({ transaction }) { return <article className="rounded-xl border border-[#e3ebe7] bg-white p-4 shadow-sm sm:p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-display text-sm font-bold">{transaction.name}</h3><p className="mt-1 text-[11px] text-[#8a9b97]">{transaction.id}</p></div><StatusBadge status={transaction.status} /></div><div className="mt-5 grid grid-cols-2 gap-4 text-xs sm:grid-cols-4"><div><p className="text-[10px] uppercase text-[#92a39f]">Role</p><p className="mt-1 font-semibold">{transaction.role}</p></div><div><p className="text-[10px] uppercase text-[#92a39f]">Other party</p><p className="mt-1 font-semibold">{transaction.otherParty}</p></div><div><p className="text-[10px] uppercase text-[#92a39f]">Amount</p><p className="mt-1 font-bold">{transaction.amount}</p></div><div><p className="text-[10px] uppercase text-[#92a39f]">Date</p><p className="mt-1 text-[#687b76]">{transaction.date}</p></div></div><Link className="mt-5 inline-flex items-center gap-1 text-xs font-bold text-[#0b776d]" to={`/dashboard/transactions/${transaction.documentId}`}>View transaction <ArrowRight size={14} /></Link></article>; }
function Transaction() { const { transactionId } = useParams(); const [transactions, setTransactions] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [search, setSearch] = useState(""); useEffect(() => { let active = true; const load = async () => { if (!auth.currentUser) { setLoading(false); return; } try { const [created, accepted] = await Promise.all([getDocs(query(collection(db, "deals"), where("creatorId", "==", auth.currentUser.uid))), getDocs(query(collection(db, "deals"), where("acceptedBy", "==", auth.currentUser.uid)))]); const documents = [...created.docs, ...accepted.docs].filter((snapshot, index, all) => all.findIndex((item) => item.id === snapshot.id) === index); if (active) setTransactions(documents.map(normalizeDeal).sort((a, b) => b.date.localeCompare(a.date))); } catch { if (active) setError("We could not load your transactions."); } finally { if (active) setLoading(false); } }; load(); return () => { active = false; }; }, []); const filtered = useMemo(() => transactions.filter((item) => `${item.name} ${item.id} ${item.otherParty}`.toLowerCase().includes(search.toLowerCase())), [transactions, search]); const transaction = transactions.find((item) => item.documentId === transactionId || item.id === transactionId); if (transactionId) return <DashboardLayout title="Transaction Details">{loading ? <div className="rounded-xl border border-[#e3ebe7] bg-white p-8 text-sm text-[#718580]">Loading transaction...</div> : transaction ? <TransactionDetails transaction={transaction} /> : <div className="rounded-xl border border-dashed border-[#cbdad4] bg-white p-10 text-center"><h2 className="font-display text-lg font-bold">Transaction not found</h2><Link className="mt-5 inline-flex text-xs font-bold text-[#0b776d]" to="/dashboard/transactions">Back to transactions</Link></div>}</DashboardLayout>; return <DashboardLayout title="Transactions"><div className="mx-auto max-w-6xl"><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[11px] font-bold uppercase tracking-[1.2px] text-[#0b776d]">Your saved deals</p><h2 className="mt-2 font-display text-2xl font-bold sm:text-3xl">Transactions</h2></div><Link className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#0b776d] px-4 py-3 text-xs font-bold text-white" to="/dashboard/create-deal"><Plus size={16} /> Create new deal</Link></div><div className="relative mb-5 max-w-sm"><Search className="absolute left-3 top-3 text-[#8da29d]" size={16} /><input className="h-10 w-full rounded-lg border border-[#cbdad4] bg-white pl-9 pr-3 text-xs outline-none focus:border-[#0b776d]" placeholder="Search transactions" value={search} onChange={(event) => setSearch(event.target.value)} /></div>{error && <p className="mb-5 rounded-lg bg-rose-50 p-3 text-xs text-rose-700">{error}</p>}{loading ? <div className="rounded-xl border border-[#e3ebe7] bg-white p-8 text-sm text-[#718580]">Loading your transactions...</div> : filtered.length ? <div className="grid gap-3">{filtered.map((item) => <TransactionCard transaction={item} key={item.documentId} />)}</div> : <div className="rounded-xl border border-dashed border-[#cbdad4] bg-white p-10 text-center"><FileText className="mx-auto text-[#8bb5a7]" size={28} /><h3 className="mt-4 font-display text-base font-bold">No transactions yet</h3><Link className="mt-5 inline-flex items-center gap-2 rounded-lg bg-[#0b776d] px-4 py-3 text-xs font-bold text-white" to="/dashboard/create-deal">Create a deal <ArrowRight size={14} /></Link></div>}</div></DashboardLayout>; }
export default Transaction;
